{
  "name": "@deliberate/protocol",
  "version": "0.0.0",
  "private": true,
  "description": "Shared contracts: state snapshot, components, diffs, intents, WebSocket messages, recording lines. Types and constants only; no runtime behaviour.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@deliberate/contracts": "workspace:*"
  }
}
